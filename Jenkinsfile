pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        sh 'echo "Olá Jenkins"'
        sh '''
          echo "Nossa primeira pipeline
          ls -la"
        '''
      }
    }     
  }
}